.keyword {
  color var(--keyword-color)
}

.no-data {
  margin $padding-md 0
  text-align center
}

.indicator {
  text-align center
  font-size $font-size-sm

  p {
    margin 0
    color var(--text-color-secondary)
  }

  .loadmore {
    padding 6px 32px
  }

  .app.mobile-layout & {
    button {
      width 100%
    }
  }
}
